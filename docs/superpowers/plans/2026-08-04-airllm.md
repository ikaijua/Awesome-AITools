# AirLLM Addition Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the AirLLM project to the Awesome-AITools bilingual READMEs under a new "LLM Inference & Deployment" / "LLM 推理与部署" section, plus a CHANGELOG entry.

**Architecture:** Insert a new category section immediately after the existing LLM training platform section in both READMEs, update each Table of Contents, append a CHANGELOG bullet, and run the README formatter.

**Tech Stack:** Markdown, Python 3, `scripts/format_readmes.py`, optional `lychee`.

## Global Constraints

- Keep English and Chinese READMEs structurally mirrored.
- Use the existing 4-column table schema.
- Run `python3 scripts/format_readmes.py` after any table edit.
- Do not modify existing tool entries or unrelated sections.

---

### Task 1: Update README.md Table of Contents

**Files:**
- Modify: `README.md:60-92`

**Interfaces:**
- Consumes: Existing `## All Categories` TOC list.
- Produces: TOC entry for the new section.

- [ ] **Step 1: Add TOC entry**

Add the new line to the `## All Categories` list, immediately after the LLM training platform entry:

```markdown
  - [LLM training platform](#llm-training-platform)
  - [LLM Inference & Deployment](#llm-inference--deployment)
```

- [ ] **Step 2: Verify TOC entry exists**

Run:

```bash
grep -n "LLM Inference & Deployment" README.md
```

Expected output contains:

```
- [LLM Inference & Deployment](#llm-inference--deployment)
```

---

### Task 2: Add LLM Inference & Deployment Section to README.md

**Files:**
- Modify: `README.md:336-341`

**Interfaces:**
- Consumes: Existing `### LLM training platform` section ending before `### Writing`.
- Produces: New `### LLM Inference & Deployment` section.

- [ ] **Step 1: Insert new section**

Insert the following block immediately after the `### LLM training platform` section and before `### Writing`:

```markdown
### LLM Inference & Deployment
| Name | Description | Links | Fees |
| --- | --- | --- | --- |
| AirLLM | A Python library that reduces LLM inference memory usage by loading one layer at a time, enabling 70B models on 4GB GPUs, 405B on 8GB, and 671B DeepSeek-V3 on ~12GB. Supports 4-bit/8-bit quantization and a wide range of open models via a single AutoModel interface. | [Github](https://github.com/lyogavin/airllm) ![GitHub Repo stars](https://img.shields.io/github/stars/lyogavin/airllm?style=social) | Free |

```

- [ ] **Step 2: Verify section content**

Run:

```bash
grep -A 3 "### LLM Inference & Deployment" README.md
```

Expected output contains the AirLLM row and the 4-column separator.

---

### Task 3: Update README-CN.md Table of Contents

**Files:**
- Modify: `README-CN.md:60-96`

**Interfaces:**
- Consumes: Existing `## 全部分类` TOC list.
- Produces: TOC entry for the new Chinese section.

- [ ] **Step 1: Add TOC entry**

Add the new line to the `## 全部分类` list, immediately after the training platform entry:

```markdown
- [大语言模型训练-评估平台](#大语言模型训练-评估平台)
- [LLM 推理与部署](#llm-推理与部署)
```

- [ ] **Step 2: Verify TOC entry exists**

Run:

```bash
grep -n "LLM 推理与部署" README-CN.md
```

Expected output contains:

```
- [LLM 推理与部署](#llm-推理与部署)
```

---

### Task 4: Add LLM 推理与部署 Section to README-CN.md

**Files:**
- Modify: `README-CN.md:336-341`

**Interfaces:**
- Consumes: Existing `### 大语言模型训练-评估平台` section ending before `### 阅读`.
- Produces: New `### LLM 推理与部署` section.

- [ ] **Step 1: Insert new section**

Insert the following block immediately after the `### 大语言模型训练-评估平台` section and before `### 阅读`:

```markdown
### LLM 推理与部署
| 名称 | 说明 | 链接 | 费用 |
| --- | --- | --- | --- |
| AirLLM | 通过逐层加载大幅降低大语言模型推理显存占用的 Python 库，可在 4GB 显存上运行 70B 模型、8GB 上运行 405B 模型、约 12GB 上运行 671B 的 DeepSeek-V3。支持 4 位/8 位量化，并通过统一的 AutoModel 接口支持众多开源模型。 | [Github](https://github.com/lyogavin/airllm) ![GitHub Repo stars](https://img.shields.io/github/stars/lyogavin/airllm?style=social) | 免费 |

```

- [ ] **Step 2: Verify section content**

Run:

```bash
grep -A 3 "### LLM 推理与部署" README-CN.md
```

Expected output contains the AirLLM row and the 4-column separator.

---

### Task 5: Update CHANGELOG.md

**Files:**
- Modify: `CHANGELOG.md:5-8`

**Interfaces:**
- Consumes: Existing `## August 2026` block.
- Produces: New bullet documenting the addition.

- [ ] **Step 1: Append changelog bullet**

Add the following bullet to the `## August 2026` list:

```markdown
- Added AirLLM (lyogavin/airllm) to new LLM Inference & Deployment section (both EN/CN)
```

- [ ] **Step 2: Verify CHANGELOG entry**

Run:

```bash
grep "AirLLM" CHANGELOG.md
```

Expected output contains the new bullet.

---

### Task 6: Format READMEs and Check Links

**Files:**
- Modify: `README.md`, `README-CN.md` (formatter may normalize whitespace/separators)

**Interfaces:**
- Consumes: Updated READMEs with new tables.
- Produces: Normalized READMEs.

- [ ] **Step 1: Run README formatter**

Run:

```bash
python3 scripts/format_readmes.py
```

Expected: script exits with code 0 and no error output.

- [ ] **Step 2: Verify formatter preserved the new sections**

Run:

```bash
grep -c "LLM Inference & Deployment" README.md
grep -c "LLM 推理与部署" README-CN.md
```

Expected: each command outputs at least `1`.

- [ ] **Step 3: Optional link check**

If `lychee` is installed, run:

```bash
lychee .
```

Expected: no new 404 errors for `github.com/lyogavin/airllm`.

---

## Self-Review Checklist

- [ ] Spec coverage: TOC entries, section content, CHANGELOG, and formatter run are all covered.
- [ ] No placeholders: all markdown content and commands are exact.
- [ ] Type consistency: section anchors match TOC links in both languages.
